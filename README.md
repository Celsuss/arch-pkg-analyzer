
# Arch pkg analyzer

A CLI utility that audits the local system's package database for anomalies, specifically focusing on validating `BUILDINFO` metadata against the currently installed binaries and checking for "drift" in shared library dependencies.
